# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/update-blocklist.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  update-blocklist:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: cachix/install-nix-action@v31
    - name: Set up git and branch
      run: |-
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"
    - name: Update blocklist
      run: nix run .#update-blocklist
    - env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      name: Create Pull Request
      run: 'gh pr create --title "chore: update blocklist" --body "Automated update
        of the blocklist."'
    timeout-minutes: 60
'on':
  schedule:
  - cron: 0 0 * * 0,1
  workflow_dispatch: {}
permissions:
  contents: write
  pull-requests: write
